[build]
  command = "pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"
  PNPM_VERSION = "11.19.0"

# The Phase 12 usage evaluator is also declared in the function source. Netlify
# runs it every three hours only for published production deployments.
